# Developer Guide
## Architecture
See `docs/ARCHITECTURE.md` and `docs/PLAN.md`.

## Testing
Run `make test` to execute the pytest suite with coverage.

## Extending Agents
Add new agents to `src/crewai_book/agents/` and configure their roles in `config/agent_configs.py`.
